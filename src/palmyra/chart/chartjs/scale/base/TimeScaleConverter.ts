
const TimeScaleConverter = (props: any) => {

    function parse() {

    }

    function format(data) {

        // function toDate(v:string) {
        //     let c = v.split('-');
        //     const m = (Number.parseInt(c[1]) % 12 + 1) +  '';
        //     c[1] = m.length == 2 ? m : '0' + m;
        //     const p = c.join('-');
        //     const df = moment(p, 'YYYY-MM-DD').unix();
        //     return df;
        // }
    }




}

export default TimeScaleConverter;